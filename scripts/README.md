# Automation Scripts

This directory contains useful shell scripts to automate tasks in Linux.

## How to run the scripts

Before running any `.sh` script, you need to grant it execution permissions.

1. Give execution permission:
```bash
   chmod +x system-updater.sh
